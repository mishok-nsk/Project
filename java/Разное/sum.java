public class sum {
    
    public static void main(String[] args) {
        /*
        int x;
        int[] a;
        a.sort();
        int i, j, k;
        for( i=0; i < a.length - 1; i++) {
            k = x - a[i];
            j = find(k , i + 1);
            if ( a[j] != a[i]) break;
        } */
    }
}
